# Assets Folder Structure

Expected local structure:

```text
assets/
  backgrounds/
    default.mp4
    sample/
  music/
  overlays/
```

Notes:
- `backgrounds/default.mp4` is preferred as base visual.
- If missing, FFmpeg falls back to a generated solid-color source.
- `music/` and `overlays/` are placeholders for future composition features.
